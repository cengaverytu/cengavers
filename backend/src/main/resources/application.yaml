spring:
  datasource:
    url: jdbc:postgresql://aws-1-ap-northeast-1.pooler.supabase.com:6543/postgres?sslmode=require&preferQueryMode=simple
    username: postgres.gadsqfpnjuqunpcgwbdt
    password: Bcdg2003.
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5
      minimum-idle: 0
      connection-timeout: 30000
      idle-timeout: 60000
      max-lifetime: 300000
      keepalive-time: 300000
      leak-detection-threshold: 20000

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: true

  sql:
    init:
      mode: always

management:
  health:
    mail:
      enabled: false
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: never

logging:
  level:
    root: INFO
    org.springframework.security: DEBUG
    com.cengavers: DEBUG
    


security:
  jwt:
    secret: "a-string-secret-at-least-256-bits-long"
    expiration: 3600000
    issuer: "ChartFlowApp"
    cookie-name: "auth"
    cookie-secure: false


auth:
  default-role: user

server:
  forward-headers-strategy: native
  servlet:
    session:
      cookie:
        secure: false
        same-site: none
  port: 8080
